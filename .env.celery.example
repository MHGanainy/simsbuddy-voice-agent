# =============================================================================
# CELERY-BASED VOICE AGENT ORCHESTRATOR - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.celery.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Redis Configuration (Message Broker + State Store)
# -----------------------------------------------------------------------------
# Local development (using docker-compose)
REDIS_URL=redis://localhost:6379/0

# Railway production (injected automatically by Redis addon)
# REDIS_URL=redis://default:password@host:port/0

# -----------------------------------------------------------------------------
# LiveKit Configuration (WebRTC Infrastructure)
# -----------------------------------------------------------------------------
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# -----------------------------------------------------------------------------
# AI Service API Keys
# -----------------------------------------------------------------------------
# Groq (LLM - llama-3.3-70b-versatile)
GROQ_API_KEY=your_groq_api_key

# AssemblyAI (Speech-to-Text)
ASSEMBLY_API_KEY=your_assemblyai_api_key

# Inworld (Text-to-Speech)
INWORLD_API_KEY=your_inworld_api_key

# -----------------------------------------------------------------------------
# Orchestrator Configuration
# -----------------------------------------------------------------------------
# Maximum number of concurrent bot sessions
MAX_BOTS=50

# Express API port
PORT=8080

# Session timeout in milliseconds (default: 30 minutes)
SESSION_TIMEOUT=1800000

# Bot startup timeout in seconds (default: 20 seconds)
BOT_STARTUP_TIMEOUT=20

# -----------------------------------------------------------------------------
# Pre-Warm Pool Configuration
# -----------------------------------------------------------------------------
# Number of pre-warmed agents to keep ready (default: 3)
PREWARM_POOL_SIZE=3

# -----------------------------------------------------------------------------
# Python Script Path
# -----------------------------------------------------------------------------
# Path to voice_assistant.py (auto-configured in Docker)
PYTHON_SCRIPT_PATH=/app/voice-assistant-project/voice_assistant.py

# For local development (outside Docker)
# PYTHON_SCRIPT_PATH=./voice-assistant-project/voice_assistant.py

# -----------------------------------------------------------------------------
# Node.js Environment
# -----------------------------------------------------------------------------
NODE_ENV=production

# -----------------------------------------------------------------------------
# Celery Configuration
# -----------------------------------------------------------------------------
# Worker concurrency (number of parallel agent spawns)
CELERY_CONCURRENCY=4

# Enable Celery debug logs (set to 1 for debugging)
CELERY_DEBUG=0

# -----------------------------------------------------------------------------
# Optional: Railway-Specific
# -----------------------------------------------------------------------------
# Railway automatically injects these (no need to set manually):
# - RAILWAY_ENVIRONMENT (production, staging, etc.)
# - RAILWAY_SERVICE_NAME
# - RAILWAY_DEPLOYMENT_ID

# -----------------------------------------------------------------------------
# Frontend Configuration (React)
# -----------------------------------------------------------------------------
# API URL for frontend to connect to orchestrator
VITE_API_URL=http://localhost:8080

# For production (Railway)
# VITE_API_URL=https://your-orchestrator.railway.app
