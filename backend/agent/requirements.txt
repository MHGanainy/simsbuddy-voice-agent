# Voice agent dependencies (Pipecat + AI services)
pipecat-ai[livekit,inworld]==0.0.92
assemblyai==0.44.3
groq==0.9.0
onnxruntime
transformers
torch

# Database dependencies for transcript storage
asyncpg==0.29.0
python-dotenv==1.0.0

# HTTP client for heartbeat billing
requests==2.31.0
